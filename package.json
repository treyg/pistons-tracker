{
  "name": "pistons-tracker",
  "version": "1.0.0",
  "description": "Pistons Tracker - Client on Netlify, Server on Railway",
  "scripts": {
    "install": "cd server && npm install",
    "start": "cd server && npm start",
    "dev:server": "cd server && npm start",
    "dev:client": "cd client && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
